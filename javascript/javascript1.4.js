var msg;
msg="<p><code>The actual script is in external script file called javascript1.4.js</code></p>";
function addNos(headVar,bodyVar)
{
//TODO: display the contents of the variable "msg"
document.write(msg);
//TODO: display the addition of two numbers
 var addition = headVar + bodyVar;
return addition;
}